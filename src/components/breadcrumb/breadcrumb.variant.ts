/* eslint-disable check-file/filename-naming-convention */
import { cva } from 'class-variance-authority'

export const breadcrumb = cva(null, {
    variants: {},
    compoundVariants: [],
    defaultVariants: {},
})
